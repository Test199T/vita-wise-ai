# Water Log API Integration - สรุปการใช้งาน

## ✅ สิ่งที่ได้ทำเสร็จแล้ว

### 1. API Functions ใน `src/services/api.ts`
- ✅ `createWaterLog()` - สร้างข้อมูลใหม่
- ✅ `getWaterLogs()` - ดึงข้อมูลทั้งหมด
- ✅ `updateWaterLog()` - อัปเดตข้อมูล
- ✅ `deleteWaterLog()` - ลบข้อมูล

### 2. Water Log Interface
```typescript
interface WaterLogItem {
  id?: string | number;
  user_id?: number;
  amount_ml: number;
  drink_type: string;
  notes?: string;
  consumed_at: string;
  created_at?: string;
  updated_at?: string;
}
```

### 3. ฟอร์มบันทึกข้อมูล
- ✅ ฟิลด์วันที่และเวลา
- ✅ ฟิลด์ปริมาณ (มิลลิลิตร)
- ✅ ฟิลด์ประเภทเครื่องดื่ม (dropdown)
- ✅ ฟิลด์หมายเหตุ
- ✅ Loading state และ error handling

### 4. ประเภทเครื่องดื่มที่รองรับ
- น้ำเปล่า (water)
- ชา (tea)
- กาแฟ (coffee)
- น้ำผลไม้ (juice)
- น้ำอัดลม (soda)
- เครื่องดื่มเกลือแร่ (sports_drink)
- อื่นๆ (other)

### 5. ฟีเจอร์ที่ใช้งานได้
- ✅ **บันทึกข้อมูลใหม่** - ส่งไปยัง `POST /water-logs`
- ✅ **อัปเดตข้อมูล** - ส่งไปยัง `PUT /water-logs/{id}`
- ✅ **ลบข้อมูล** - ส่งไปยัง `DELETE /water-logs/{id}`
- ✅ **ดึงข้อมูล** - ส่งไปยัง `GET /water-logs`
- ✅ **แสดงข้อมูล** - แสดงรายการจากฐานข้อมูล
- ✅ **Loading states** - แสดงสถานะการโหลด
- ✅ **Error handling** - จัดการข้อผิดพลาด
- ✅ **Toast notifications** - แจ้งเตือนผลลัพธ์

## 🎯 วิธีการใช้งาน

### 1. บันทึกข้อมูลใหม่
1. คลิกปุ่ม "เพิ่มบันทึก"
2. กรอกข้อมูลในฟอร์ม:
   - วันที่และเวลา
   - ปริมาณ (มิลลิลิตร)
   - ประเภทเครื่องดื่ม
   - หมายเหตุ (ไม่บังคับ)
3. คลิก "บันทึก"
4. ระบบจะส่งข้อมูลไปยัง API และแสดงผลลัพธ์

### 2. แก้ไขข้อมูล
1. คลิกปุ่ม "แก้ไข" ในรายการที่ต้องการ
2. ฟอร์มจะแสดงข้อมูลเดิม
3. แก้ไขข้อมูลตามต้องการ
4. คลิก "อัปเดต"
5. ระบบจะส่งข้อมูลไปยัง API

### 3. ลบข้อมูล
1. คลิกปุ่ม "ลบ" ในรายการที่ต้องการ
2. ยืนยันการลบใน dialog
3. ระบบจะลบข้อมูลจากฐานข้อมูล

### 4. รีเฟรชข้อมูล
1. คลิกปุ่ม "รีเฟรช" ด้านบน
2. ระบบจะดึงข้อมูลล่าสุดจาก API

## 🔧 API Endpoints ที่ใช้

### POST /water-logs
```json
{
  "amount_ml": 250,
  "drink_type": "water",
  "notes": "Morning hydration",
  "consumed_at": "2024-01-15T08:30:00Z"
}
```

### GET /water-logs
- ดึงข้อมูลทั้งหมดของผู้ใช้ปัจจุบัน
- ใช้ Bearer token สำหรับ authentication

### PUT /water-logs/{id}
```json
{
  "amount_ml": 300,
  "drink_type": "tea",
  "notes": "Updated note",
  "consumed_at": "2024-01-15T09:00:00Z"
}
```

### DELETE /water-logs/{id}
- ลบข้อมูลตาม ID
- ไม่ต้องส่ง request body

## 🎨 UI/UX Features

### Loading States
- ปุ่มแสดง "กำลังบันทึก..." เมื่อกำลังส่งข้อมูล
- ปุ่มแสดง "กำลังลบ..." เมื่อกำลังลบข้อมูล
- แสดง spinner เมื่อกำลังโหลดข้อมูล

### Error Handling
- แสดง toast notification เมื่อเกิดข้อผิดพลาด
- แสดง error message ที่เป็นภาษาไทย
- Fallback ไปใช้ localStorage หาก API ไม่พร้อมใช้งาน

### Data Display
- แสดงประเภทเครื่องดื่มเป็นภาษาไทย
- แสดงวันที่และเวลาในรูปแบบที่อ่านง่าย
- แสดงหมายเหตุในกรอบแยกต่างหาก

## 🧪 การทดสอบ

### ปุ่มทดสอบ API
- **ทดสอบ POST** - ส่งข้อมูลตัวอย่างไปยัง API
- **ทดสอบ GET** - ดึงข้อมูลจาก API
- แสดงผลลัพธ์ในรูปแบบ JSON

### Console Logging
- เปิด Developer Tools (F12)
- ดู Console tab สำหรับ logs รายละเอียด
- ดู Network tab สำหรับ HTTP requests

## 🔒 Authentication

- ใช้ JWT token จาก localStorage
- ส่ง Bearer token ใน Authorization header
- ตรวจสอบ token validity ก่อนเรียก API

## 📱 Responsive Design

- ฟอร์มปรับขนาดตามหน้าจอ
- Grid layout สำหรับฟิลด์ต่างๆ
- ปุ่มและ UI elements ปรับขนาดตามอุปกรณ์

## 🚀 การใช้งานจริง

1. **เข้าสู่ระบบ** - ต้องมี JWT token ที่ถูกต้อง
2. **ไปที่หน้า Water Log** - ระบบจะโหลดข้อมูลจาก API อัตโนมัติ
3. **บันทึกข้อมูล** - ใช้ฟอร์มเพื่อบันทึกข้อมูลใหม่
4. **จัดการข้อมูล** - แก้ไขหรือลบข้อมูลตามต้องการ
5. **ดูประวัติ** - ดูรายการข้อมูลที่บันทึกไว้

## ⚠️ หมายเหตุ

- ข้อมูลจะถูกบันทึกลงฐานข้อมูลจริง
- หาก API ไม่พร้อมใช้งาน ระบบจะใช้ข้อมูลจาก localStorage เป็น fallback
- ต้องแน่ใจว่า backend server ทำงานอยู่ที่ port 3000
- ต้องมี JWT token ที่ถูกต้องสำหรับการเรียก API
