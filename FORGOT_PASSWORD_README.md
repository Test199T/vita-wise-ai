# ระบบลืมรหัสผ่าน (Forgot Password System) - Frontend Mock API

## 🎯 สถานะปัจจุบัน

**Frontend พร้อมใช้งาน ✅**
- หน้า ForgotPassword (`/forgot-password`)
- หน้า ResetPassword (`/reset-password?token=xxx`)
- Mock API methods ใน `api.ts`
- Routes ใน `App.tsx`
- ลิงก์ "ลืมรหัสผ่าน?" ในหน้า Login

## 📁 ไฟล์ที่สร้าง/แก้ไข

### ไฟล์ใหม่
- ✅ `src/pages/ForgotPassword.tsx` - หน้าลืมรหัสผ่าน
- ✅ `src/pages/ResetPassword.tsx` - หน้าตั้งรหัสผ่านใหม่

### ไฟล์ที่แก้ไข
- ✅ `src/services/api.ts` - เพิ่ม Mock API methods
- ✅ `src/App.tsx` - เพิ่ม routes

### ไฟล์ที่มีอยู่แล้ว
- ✅ `src/pages/Login.tsx` - มีลิงก์ "ลืมรหัสผ่าน?" อยู่แล้ว

## 🔧 Mock API Methods

### 1. `forgotPassword(email: string)`
```typescript
// ตรวจสอบอีเมลที่มีอยู่ในระบบ
const validEmails = [
  "test@example.com",
  "methasphoynxk90@gmail.com",
  "testuser789@gmail.com",
  "zoomgamer807@gmail.com",
  "asngiun@gmail.com",
  "johndoe123@gmail.com",
  "postman_test@gmail.com",
  "ppansiun@gmail.com"
];

// ส่งกลับ success หรือ throw "User not found"
```

### 2. `validateResetToken(token: string)`
```typescript
// ตรวจสอบ token (ต้องมีความยาว > 10 ตัวอักษร)
if (token && token.length > 10) {
  return true; // ✅ Valid
} else {
  throw new Error('Invalid or expired token'); // ❌ Invalid
}
```

### 3. `resetPassword(token: string, newPassword: string)`
```typescript
// ตรวจสอบ token และ password
if (!token || token.length < 10) {
  throw new Error('Invalid or expired token');
}

if (newPassword.length < 8) {
  throw new Error('Password too short');
}

// ✅ Success
```

## 🧪 วิธีการทดสอบ

### 1. ทดสอบการส่งอีเมล
```
1. ไปที่ /forgot-password
2. กรอกอีเมล: methasphoynxk90@gmail.com
3. กด "ส่งอีเมลรีเซ็ตรหัสผ่าน"
4. ควรเห็น Success state ✅
```

### 2. ทดสอบการรีเซ็ตรหัสผ่าน
```
1. ไปที่ /reset-password?token=valid-token-12345
2. กรอกรหัสผ่านใหม่: NewPassword123
3. ยืนยันรหัสผ่าน: NewPassword123
4. กด "ตั้งรหัสผ่านใหม่"
5. ควรเห็น Success state ✅
```

### 3. ทดสอบ Error Cases
```
- อีเมลไม่ถูกต้อง: ควรแสดง error
- Token ไม่ถูกต้อง: ควรแสดง error
- รหัสผ่านไม่ตรงตามข้อกำหนด: ควรแสดง error
- รหัสผ่านไม่ตรงกัน: ควรแสดง error
```

## 📋 ข้อกำหนดรหัสผ่าน

รหัสผ่านใหม่ต้องมี:
- ✅ อย่างน้อย 8 ตัวอักษร
- ✅ ตัวพิมพ์ใหญ่อย่างน้อย 1 ตัว
- ✅ ตัวพิมพ์เล็กอย่างน้อย 1 ตัว
- ✅ ตัวเลขอย่างน้อย 1 ตัว

## 🔗 Routes ที่เพิ่ม

```typescript
// ใน App.tsx
<Route path="/forgot-password" element={<ForgotPassword />} />
<Route path="/reset-password" element={<ResetPassword />} />
```

## 🎨 UI/UX Features

### ForgotPassword Page
- ✅ ฟอร์มกรอกอีเมล
- ✅ การตรวจสอบรูปแบบอีเมล
- ✅ Loading states
- ✅ Error handling
- ✅ Success state
- ✅ Navigation กลับไปหน้า Login

### ResetPassword Page
- ✅ ฟอร์มตั้งรหัสผ่านใหม่
- ✅ การตรวจสอบ Token
- ✅ การตรวจสอบความแข็งแกร่งของรหัสผ่าน
- ✅ Show/Hide password
- ✅ Success state
- ✅ Error states สำหรับ Token ไม่ถูกต้อง

## 🔄 ขั้นตอนต่อไป (สำหรับคุณ)

เมื่อคุณทำ Backend เสร็จแล้ว:

### 1. แทนที่ Mock API
```typescript
// ใน api.ts - แทนที่ methods เหล่านี้
async forgotPassword(email: string): Promise<void> {
  // เรียก Backend API จริง
  const response = await fetch('/api/auth/forgot-password', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ email })
  });
  // ... handle response
}
```

### 2. Backend Endpoints ที่ต้องมี
```typescript
POST /api/auth/forgot-password
POST /api/auth/validate-reset-token
POST /api/auth/reset-password
```

### 3. การเชื่อมต่อฐานข้อมูล
- ตรวจสอบอีเมลในตาราง `users`
- สร้างและบันทึก reset token
- อัปเดตรหัสผ่านใหม่

## 📝 หมายเหตุ

- **Mock API** ใช้สำหรับการทดสอบ Frontend เท่านั้น
- **Frontend พร้อมใช้งาน** ทุกอย่างแล้ว
- **Backend** ต้องสร้างตาม API endpoints ที่กำหนด
- **Email Service** ต้องตั้งค่า SMTP ใน Backend

---

## 🎉 สรุป

**Frontend ระบบลืมรหัสผ่านพร้อมใช้งานแล้ว!**

คุณสามารถ:
1. ทดสอบ UI/UX ได้เลย
2. ดูการทำงานของ Mock API
3. ทำ Backend ตาม API endpoints ที่กำหนด
4. เชื่อมต่อ Frontend กับ Backend เมื่อพร้อม

**เมื่อ Backend เสร็จแล้ว ระบบจะทำงานได้จริงทันที! 🚀**
