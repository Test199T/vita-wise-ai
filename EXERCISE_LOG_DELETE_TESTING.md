# คู่มือการทดสอบฟีเจอร์การลบข้อมูลการออกกำลังกาย

## 🧪 การทดสอบขั้นพื้นฐาน

### 1. ทดสอบการโหลดข้อมูลจาก Backend
1. เปิดหน้า ExerciseLog
2. ดู Console (F12 → Console)
3. ควรเห็นข้อความ:
   ```
   📥 โหลดข้อมูลการออกกำลังกายจาก Backend...
   ✅ โหลดข้อมูลจาก Backend สำเร็จ: X รายการ
   📥 ข้อมูลดิบจาก Backend: [...]
   🆔 แปลง ID: { originalId: X, sessionId: "X" }
   🔄 ข้อมูลที่แปลงแล้ว: [...]
   ```

### 2. ทดสอบการลบข้อมูล
1. คลิกปุ่ม "ลบ" ที่รายการการออกกำลังกาย
2. ยืนยันการลบใน dialog
3. ดู Console ควรเห็น:
   ```
   🗑️ เริ่มลบข้อมูลการออกกำลังกาย: [ID]
   📋 รายละเอียดข้อมูลที่จะลบ: {...}
   🗑️ Deleting exercise log entry... [ID]
   🌐 API URL: http://localhost:3000/exercise-log/[ID]
   🔑 Token found: [token]...
   📡 Delete exercise log API response: {...}
   ✅ Exercise log deleted successfully from backend
   ✅ ลบข้อมูลจาก Backend สำเร็จ
   ```

## 🔍 การแก้ไขปัญหา

### ปัญหา: ลบข้อมูลไม่ออก

#### ตรวจสอบที่ 1: ID ที่ส่งไป Backend
- ดู Console ว่า ID ที่ส่งไปถูกต้องหรือไม่
- ตรวจสอบว่า `log.id` จาก Backend มีค่าหรือไม่

#### ตรวจสอบที่ 2: API Endpoint
- ตรวจสอบว่า Backend มี endpoint `DELETE /exercise-log/{id}` หรือไม่
- ตรวจสอบ URL ที่ส่งไป: `http://localhost:3000/exercise-log/{id}`

#### ตรวจสอบที่ 3: Authentication
- ตรวจสอบว่า token ถูกส่งไปหรือไม่
- ดู Console: `🔑 Token found: [token]...`

#### ตรวจสอบที่ 4: Backend Response
- ดู Console: `📡 Delete exercise log API response: {...}`
- ตรวจสอบ status code และ response body

## 🛠️ การ Debug

### 1. เปิด Developer Tools
- กด F12 หรือคลิกขวา → Inspect
- ไปที่แท็บ Console

### 2. ดู Network Tab
- ไปที่แท็บ Network
- ลบข้อมูลการออกกำลังกาย
- ดู request DELETE ที่ส่งไป
- ตรวจสอบ:
  - URL
  - Headers (Authorization)
  - Response status
  - Response body

### 3. ตรวจสอบ Backend Logs
- ดู console ของ Backend server
- ตรวจสอบว่าได้รับ request หรือไม่
- ตรวจสอบ error messages

## 📋 ข้อมูลที่ต้องตรวจสอบ

### Frontend State
```javascript
// ตรวจสอบข้อมูลใน localStorage
localStorage.getItem('exercise_logs')

// ตรวจสอบ state
console.log('Sessions:', sessions)
console.log('Deleting ID:', deletingId)
```

### Backend Data
```javascript
// ตรวจสอบข้อมูลใน Backend
// GET /exercise-log
// ดูว่ามีข้อมูลและมี ID หรือไม่
```

## 🚨 สาเหตุที่อาจทำให้ลบไม่ออก

### 1. ID ไม่ตรงกัน
- Frontend สร้าง ID เอง แต่ Backend ต้องการ ID จริง
- ข้อมูลจาก Backend ไม่มี `id` field

### 2. API Endpoint ไม่ถูกต้อง
- Backend ไม่มี endpoint `DELETE /exercise-log/{id}`
- URL format ไม่ถูกต้อง

### 3. Authentication ผิดพลาด
- Token หมดอายุ
- Token ไม่ถูกส่งไป
- Backend ไม่ยอมรับ token

### 4. Backend Error
- Database connection error
- Permission denied
- Data not found

## ✅ การแก้ไขที่แนะนำ

### 1. ตรวจสอบ Backend
- ตรวจสอบว่า Backend มี endpoint `DELETE /exercise-log/{id}` หรือไม่
- ตรวจสอบ database schema ว่ามี `id` field หรือไม่

### 2. ตรวจสอบ Frontend
- ตรวจสอบว่า `log.id` จาก Backend มีค่าหรือไม่
- ตรวจสอบว่า ID ที่ส่งไป Backend ถูกต้องหรือไม่

### 3. ตรวจสอบ Authentication
- ตรวจสอบว่า token ถูกส่งไปหรือไม่
- ตรวจสอบว่า token ยังไม่หมดอายุ

## 📞 การขอความช่วยเหลือ

หากยังแก้ไขไม่ได้ กรุณาแจ้ง:
1. Error message ที่เห็นใน Console
2. Network request ที่ส่งไป Backend
3. Response จาก Backend
4. Backend logs (ถ้ามี)
